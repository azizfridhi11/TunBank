## Packages
recharts | Financial data visualization charts
framer-motion | Smooth page transitions and UI animations
date-fns | Date formatting for transactions
react-hook-form | Form state management
@hookform/resolvers | Zod integration for forms
clsx | Conditional class merging
tailwind-merge | Tailwind class merging

## Notes
Authentication uses session cookies (credentials: "include")
Financial amounts are decimal strings in DB, parse to float for charts
